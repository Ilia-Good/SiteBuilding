# HTML Шаблон Генератор 🎨

## Описание

Сервис `HtmlTemplateGenerator` создаёт профессиональный HTML шаблон мини-сайта с встроенной формой обратной связи через Formspree.

## Что включено?

- ✅ Современный дизайн с Bootstrap 5
- ✅ Форма обратной связи через Formspree
- ✅ Адаптивный дизайн (работает на всех устройствах)
- ✅ Инструкции по настройке Formspree
- ✅ Кнопка для добавления дополнительной формы
- ✅ Темизация интерфейса
- ✅ Секция с преимуществами сайта
- ✅ Место для изображения
- ✅ Экранирование HTML для безопасности

## Использование в коде

### Через контроллер (POST)

```bash
curl -X POST http://localhost/builder/generate-template \
  -H "Content-Type: application/json" \
  -d '{
    "siteName": "Мой Сайт",
    "siteDescription": "Добро пожаловать на наш новый сайт!"
  }'
```

**Ответ:**
```json
{
  "html": "<!DOCTYPE html>..."
}
```

### Скачивание как файл (POST)

```bash
curl -X POST http://localhost/builder/download-template \
  -H "Content-Type: application/json" \
  -d '{
    "siteName": "MyWebsite",
    "siteDescription": "Welcome to my website"
  }' \
  --output MyWebsite.html
```

### JavaScript пример

```javascript
async function generateSite() {
  const response = await fetch('/builder/generate-template', {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json'
    },
    body: JSON.stringify({
      siteName: 'Мой Сайт',
      siteDescription: 'Добро пожаловать!'
    })
  });
  
  const data = await response.json();
  console.log(data.html); // Ваш HTML шаблон
}
```

## Структура сгенерированного сайта

```
┌─────────────────────────┐
│    Навигационное меню   │
├─────────────────────────┤
│                         │
│    Главная секция       │
│   (Hero Section)        │
│                         │
├─────────────────────────┤
│                         │
│   🖼️ Место для         │
│      изображения        │
│                         │
├─────────────────────────┤
│                         │
│  ⚡ Особенности сайта   │
│  📱 (в три колонки)    │
│  🔒                     │
│                         │
├─────────────────────────┤
│                         │
│  📬 Форма обратной      │
│     связи через         │
│     Formspree           │
│                         │
│  👉 Инструкция по      │
│     настройке           │
│                         │
├─────────────────────────┤
│  ⚙️ Дополнительные     │
│     функции             │
│  [➕ Добавить форму]    │
│  [🎨 Переключить тему] │
├─────────────────────────┤
│       Footer            │
└─────────────────────────┘
```

## Настройка Formspree

1. **Перейти на сайт:** https://formspree.io
2. **Создать новую форму** и укажите почту пользователя
3. **Получить ID формы** (выглядит как: `f/ABC123DEF`)
4. **Заменить в HTML:**
   - Найти: `action="https://formspree.io/f/YOUR_FORM_ID"`
   - Заменить на: `action="https://formspree.io/f/ВАШ_ID"`
5. **Сохранить и тестировать**

## Поля формы

| Поле | Тип | Описание |
|------|-----|---------|
| name | text | Имя отправителя |
| email | email | Email отправителя |
| subject | text | Тема сообщения |
| message | textarea | Текст сообщения |

## Безопасность

- ✅ HTML спецсимволы экранируются
- ✅ Email валидируется
- ✅ CSRF токены поддерживаются
- ✅ Данные отправляются через POST
- ✅ Formspree обеспечивает защиту от спама

## Стилизация

HTML использует встроенный CSS с переменными цвета:

```css
:root {
    --primary-color: #4f46e5; /* Фиолетовый */
}
```

Для изменения цветовой схемы замените значение `--primary-color`.

### Доступные цвета:
- 🔵 `#4f46e5` - Индиго (по умолчанию)
- 🟣 `#8b5cf6` - Фиолетовый
- 🔴 `#dc2626` - Красный
- 🟢 `#10b981` - Зелёный
- 🔷 `#0ea5e9` - Голубой

## Примеры использования

### Пример 1: Сайт портфолио

```csharp
var html = _templateGenerator.GenerateTemplate(
    userEmail: "portfolio@example.com",
    siteName: "Портфолио разработчика",
    siteDescription: "Здесь находятся мои лучшие проекты"
);
```

### Пример 2: Бизнес сайт

```csharp
var html = _templateGenerator.GenerateTemplate(
    userEmail: "contact@mybusiness.com",
    siteName: "Моё агентство",
    siteDescription: "Предоставляем качественные услуги для вашего бизнеса"
);
```

### Пример 3: Посты в блоге

```csharp
var html = _templateGenerator.GenerateTemplate(
    userEmail: "hello@blog.com",
    siteName: "Технический блог",
    siteDescription: "Статьи о веб-разработке и программировании"
);
```

## Совет 💡

Используйте **CSS внутри** сгенерированного HTML для дополнительной персонализации жанра, текстуры, фона, и эффектов.

## Поддежка

Если у вас есть вопросы или проблемы с генератором - создайте issue в репозитории проекта.

---

**Версия:** 1.0  
**Последнее обновление:** 2026-02-09  
**Язык:** C#/.NET 9.0
