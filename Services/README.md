# 🎨 HTML Template Generator

Генератор профессиональных HTML шаблонов мини-сайтов с встроенной формой обратной связи.

## 📋 Содержание

- [Возможности](#возможности)
- [Установка](#установка)
- [Использование](#использование)
- [API Endpoints](#api-endpoints)
- [Примеры](#примеры)
- [Структура](#структура)
- [Безопасность](#безопасность)

## ✨ Возможности

- ✅ **Современный дизайн** - использует Bootstrap 5
- ✅ **Адаптивный** - работает на всех устройствах
- ✅ **Форма обратной связи** - интегрирована с Formspree
- ✅ **Инструкции** - подробные описания по настройке
- ✅ **Email валидация** - автоматически подставляет email пользователя
- ✅ **Кастомизируемый** - легко редактировать HTML/CSS
- ✅ **Безопасный** - экранирование HTML спецсимволов
- ✅ **Интерактивный** - кнопки для добавления форм и смены темы

## 🚀 Установка

### 1. Файлы добавлены в проект

```
Services/
├── HtmlTemplateGenerator.cs       # Основной сервис
└── TEMPLATE_USAGE.md             # Документация

Views/
└── Builder/
    └── TemplateGenerator.cshtml   # UI компонент

wwwroot/
└── js/
    └── template-generator.js      # JavaScript интеграция
```

### 2. Регистрация в Program.cs

```csharp
using SiteBuilder.Services;

// В разделе сервисов:
builder.Services.AddSingleton<HtmlTemplateGenerator>();
```

### 3. Обновление BuilderController

Добавлены методы:
- `GenerateTemplate(TemplateRequest)` - POST запрос для генерирования
- `DownloadTemplate(TemplateRequest)` - POST запрос для скачивания
- `TemplateGenerator()` - GET для отображения UI

## 📖 Использование

### Через веб-интерфейс

1. Перейдите на: `http://localhost/builder/template`
2. Введите название и описание сайта
3. Выберите действие:
   - 👁️ **Превью** - посмотреть результат
   - ⬇️ **Скачать HTML** - загрузить файл
   - 🔗 **Открыть в новой вкладке** - открыть в браузере

### Через API (JavaScript)

```javascript
// Загрузить скрипт
<script src="/js/template-generator.js"></script>

// Создать экземпляр генератора
const generator = new TemplateGenerator();

// Генерировать HTML
const html = await generator.generateTemplate(
    'Мой сайт',
    'Добро пожаловать!'
);

// Скачать файл
await generator.downloadTemplate(
    'Мой сайт',
    'Добро пожаловать!',
    'my-site'
);
```

### Через API (Fetch)

```javascript
fetch('/builder/generate-template', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({
        siteName: 'Мой Сайт',
        siteDescription: 'Добро пожаловать!'
    })
})
.then(r => r.json())
.then(data => console.log(data.html));
```

### Через C# код

```csharp
// В контроллере или сервисе
var generator = new HtmlTemplateGenerator();
var html = generator.GenerateTemplate(
    userEmail: "user@example.com",
    siteName: "Мой Сайт",
    siteDescription: "Добро пожаловать!"
);

// Вернуть HTML
return Content(html, "text/html");
```

## 🔌 API Endpoints

### POST `/builder/generate-template`

Генерирует HTML и возвращает как JSON.

**Запрос:**
```json
{
    "siteName": "Мой Сайт",
    "siteDescription": "Добро пожаловать!"
}
```

**Ответ:**
```json
{
    "html": "<!DOCTYPE html>..."
}
```

**Коды ошибок:**
- `200` - успех
- `400` - ошибка валидации
- `401` - не авторизован

---

### POST `/builder/download-template`

Генерирует HTML и возвращает как файл.

**Запрос:**
```json
{
    "siteName": "my-site",
    "siteDescription": "Welcome!"
}
```

**Ответ:**
- Скачивается файл `my-site.html`

---

### GET `/builder/template`

Открывает UI генератора.

**Ответ:**
- HTML страница с интерфейсом

## 📝 Примеры

### Пример 1: Портфолио разработчика

```csharp
var html = generator.GenerateTemplate(
    userEmail: "dev@example.com",
    siteName: "Портфолио разработчика",
    siteDescription: "Full-Stack разработчик с опытом 5+ лет"
);
```

### Пример 2: Малый бизнес

```csharp
var html = generator.GenerateTemplate(
    userEmail: "contact@mybiz.com",
    siteName: "Парикмахерская 'Стиль'",
    siteDescription: "Лучшие услуги парикмахерского искусства в городе"
);
```

### Пример 3: Блог

```javascript
await generator.generateAndPreview(
    'Технический блог',
    'Статьи о веб-разработке и программировании'
);
```

## 🏗️ Структура сгенерированного HTML

```html
<!DOCTYPE html>
<html>
  <head>
    <!-- Метатеги, стили, Bootstrap -->
  </head>
  <body>
    <!-- Навигационное меню -->
    <nav class="navbar">
      🚀 Название сайта
    </nav>
    
    <!-- Герой секция -->
    <section class="hero-section">
      <h1>Название сайта</h1>
      <p>Описание</p>
    </section>
    
    <!-- Плейсхолдер для изображения -->
    <div class="image-placeholder">
      🖼️ Ваше изображение
    </div>
    
    <!-- Особенности (3 колонки) -->
    <section class="features-section">
      ⚡ Быстрый
      📱 Адаптивный
      🔒 Защищённый
    </section>
    
    <!-- Форма обратной связи -->
    <section class="form-section">
      📬 Напишите нам
      👉 Инструкция по Formspree
      <form action="https://formspree.io/f/YOUR_FORM_ID">
        <input name="name" placeholder="Имя">
        <input name="email" placeholder="Email">
        <input name="subject" placeholder="Тема">
        <textarea name="message" placeholder="Сообщение"></textarea>
        <button type="submit">✉️ Отправить</button>
      </form>
    </section>
    
    <!-- Дополнительные функции -->
    <section>
      [➕ Добавить форму контактов]
      [🎨 Переключить тему]
    </section>
    
    <!-- Footer -->
    <footer>
      © 2026 Мой Сайт. Создано с помощью SiteBuilder
    </footer>
    
    <!-- JavaScript -->
  </body>
</html>
```

## 🔒 Безопасность

- ✅ HTML спецсимволы экранируются (`&`, `<`, `>`, `"`, `'`)
- ✅ Email валидируется на сервере
- ✅ Форма использует POST метод
- ✅ Formspree предоставляет защиту от спама
- ✅ Требует авторизации (атрибут `[Authorize]`)
- ✅ Нет SQL injections (не используется БД для этой функции)

## 🎨 Кастомизация

### Расширение инструкций

В файле `HtmlTemplateGenerator.cs`, методе `GenerateTemplate()`:

```csharp
template.AppendLine("    <div class=\"instruction-box\">");
template.AppendLine("        <div class=\"instruction-title\">ВАШ ТЕКСТ</div>");
template.AppendLine("        <p class=\"instruction-text\">ОПИСАНИЕ</p>");
template.AppendLine("    </div>");
```

### Изменение цвета

Найти строку:
```csharp
template.AppendLine("        :root { --primary-color: #4f46e5; }");
```

Заменить `#4f46e5` на нужный цвет.

### Добавление новых элементов

Просто добавьте строки с `template.AppendLine()` в нужное место.

## 🤝 Интеграция с Formspree

1. Перейти на https://formspree.io
2. Создать новую форму
3. Указать email пользователя
4. Получить Form ID (вид: `f/ABC123xyz`)
5. В HTML найти: `action="https://formspree.io/f/YOUR_FORM_ID"`
6. Заменить `YOUR_FORM_ID` на полученный ID
7. Тестировать форму

## 📊 Статистика

- **Размер HTML**: ~25-30 KB
- **Зависимости**: Bootstrap 5 (CDN)
- **Поддерживаемые браузеры**: Chrome, Firefox, Safari, Edge (все современные)
- **Мобильная оптимизация**: Да

## 🐛 Решение проблем

### Форма не отправляется
- Проверьте, что заменили `YOUR_FORM_ID` на реальный ID
- Убедитесь, что email подтверждён в Formspree
- Проверьте консоль браузера (F12) на ошибки

### Email не подставляется
- Убедитесь, что вы авторизованы (через Google)
- Проверьте, что Google профиль содержит email

### Стили не загружаются
- Проверьте интернет соединение
- Bootstrap CDN может быть заблокирован

## 📞 Поддержка

Вопросы? Создайте issue в репозитории:
https://github.com/Ilia-Good/SiteBuilding/issues

---

**Версия:** 1.0  
**Автор:** SiteBuilder Team  
**Лицензия:** MIT  
**Последнее обновление:** 2026-02-09
